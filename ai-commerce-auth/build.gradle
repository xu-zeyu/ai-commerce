// 公共模块配置
description = '登录认证模块'

apply from: "${rootDir}/buildSrc/src/main/groovy/com.aicommerce.conventions.spring-boot.gradle"

// 这是一个库模块，不需要可执行的 bootJar
bootJar {
    enabled = false
}

jar {
    enabled = true
}

dependencies {
    implementation(project(":ai-commerce-common"))
    implementation(project(":ai-commerce-start:ai-commerce-starter-cache"))
    implementation(project(":ai-commerce-start:ai-commerce-starter-mybatis"))
    api 'cn.dev33:sa-token-spring-boot3-starter:1.44.0'
    implementation 'cn.dev33:sa-token-redis-jackson:1.44.0'
    implementation 'org.apache.commons:commons-pool2'
    implementation 'cn.dev33:sa-token-redis-template:1.44.0'
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok:1.18.26'
}

// 测试配置
test {
    useJUnitPlatform()
}